name: ${artifactId}
main: ${groupId}.${artifactId}
version: ${project.version}
api-version: ${project.bukkitAPIVersion}

authors:
    - Shade / Zren (CellWar, Towny)
    - ElgarL (TownyAdvanced)
    - LlmDl (TownyAdvanced)
    - TheFlagCourier (FlagWar)
    - Various Contributors

website: ${organization.url}

prefix: FlagWar

description: ${description}

folia-supported: true

softdepend:
    - HolographicDisplays

depend:
    - Towny

commands:
    PhaseAdvance:
        description: Advances the phase of a war. E.g. Pre-Flag --> Flag
        usage: /<command> <TownName>
        aliases:
            - pa
